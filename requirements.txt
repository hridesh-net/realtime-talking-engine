fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.0
python-dotenv>=1.0
# Multipart request bodies: the browser's recording chunks. Starlette imports
# it lazily, so without it `request.form()` raises rather than 400s.
python-multipart>=0.0.9
psycopg[binary]>=3.2.10
psycopg-pool>=3.2.6
boto3>=1.35
google-genai>=1.0
openai>=1.30

# dev
pytest>=8.0
pytest-asyncio>=0.23
httpx>=0.27
ruff>=0.4
