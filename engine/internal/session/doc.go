// Package session implements the actor loop, state machine, turn lifecycle,
// timers, and truncation accounting, plus the concurrency-safe
// create/look-up/stop session registry (Manager) and the HTTP surface
// (Handler) that drives it.
package session
