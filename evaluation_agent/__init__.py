"""Manager assessment: what the manager was supposed to do, and whether they did.

Sibling of ``candidate_agent``. Imports ``llm`` and nothing else — never the
control plane, never another agent package.
"""
